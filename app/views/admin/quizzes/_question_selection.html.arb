panel title do
  table_for questions do
    column "Select" do |it|
      check_box_tag "question_id[#{it.id}]", it.id, true
    end
    column :id do |it|
      link_to it.id, admin_question_path(it)
    end
    column :title
  end

  div do
    submit_tag cta
  end if cta

end
